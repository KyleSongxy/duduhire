# Configure hosted values through Sites. Never expose these keys in browser code.
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=

# Per Worker instance safety limit. Add an account-level gateway/rate limiter
# before enabling paid model traffic at scale.
AI_REQUESTS_PER_10_MINUTES=10
