# 2026年9月14日仓库清理

本次仅清理本地文件和维护文档，不变更页面文案、生产服务、数据库或付费接口。仓库当时没有任何 Git 提交，文件全部未跟踪，因此删除内容先移至仓库外可恢复目录。

## 清理范围

- 移除 public 中未被 Web 源码、CSS 或入口 HTML 引用的8份历史影片/海报：brand-film V5、V6、V7及对应海报，product-motion V1及海报。当前首页视频和工作方式 V2 4K保持原文件。
- 移除 product-motion 的 V1、V2 4K `frames` 逐帧缓存以及根部 Python 字节码缓存；渲染脚本会重新生成。保留素材、可编辑工程、成片、静帧与 QA 记录。
- 移除过期的 `docs/UI_COPY_AUDIT_2026-09-05.md`。当前功能以 PRODUCT 为准，后续会议需求保留在 output/meetings。
- 将 AI 流程历史验收移至 `docs/archive/`，保留历史结果和知识来源；知识研究仍被 AI_FLOWS 引用，未删除。
- 删除 `domainKnowledge.ts` 中无任何引用的 `KNOWLEDGE_VERSION` 常量。保留实际用于生成与匹配的知识条目、来源和提示版本。

共移出2,656个文件、8,664,251,507字节（约8.07 GiB）。这是仓库体积减少；备份仍占磁盘空间，不代表释放等量磁盘。

## 恢复

恢复目录：`/Users/xiangyusong/.local/share/duduhire/cleanup/20260914-210739`。

其中 `manifest.json` 逐文件记录原相对路径、字节数与 SHA-256。目录结构与仓库一致，按需复制选定文件回相同相对路径即可；恢复前核对目标是否已有新文件，不覆盖后续改动。不要批量恢复旧 public 成片到新发行包，除非明确需要回退。

## 保留依据

- 历史 cinematic 工具仍提供现役素材构建使用的首页截图及引擎探针图，不能整目录删除。
- `public/service-worker.js` 是旧站 Fangxu 缓存退役入口，无当前注册调用仍有外部浏览器用途。
- 001–009迁移、运维发布/回滚脚本、模拟数据批次及部署记录有运行和追溯用途，全部保留。
- 未实施9月13日会议的页面措辞，也不把待定收费、验证与交付要求写成已实现。

## 核心文档维护

增加 docs/README.md 作为总入口，更新 README、PRODUCT、ARCHITECTURE、API、AUTHENTICATION、SECURITY、TESTING、RUNBOOK；修正手机号模式、邮箱停用、登录资格预检查、009迁移、结构化匹配条件及私密示例边界。历史工具 README 加入明确的版本说明。增加 `check:repository` 并纳入 `npm run check`，防止核心文档链接和静态资源引用失效。

## 验证

`npm run check` 通过：Web 18项、API 200项测试，0失败、0跳过；前后端 lint 和 build 通过。核心相对文档链接及25处静态资源引用通过检查。新增检查也曾准确报告缺失的 docs/README.md，创建索引后恢复通过。

未执行 PostgreSQL 集成测试或浏览器 E2E；本次没有页面行为或布局改动，验证聚焦引用、现有测试和构建。未执行生产检查、真实短信、邮件投递或付费模型请求。
