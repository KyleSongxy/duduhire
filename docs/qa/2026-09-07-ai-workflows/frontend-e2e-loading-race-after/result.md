匹配加载时序修复后的回归

命令：npm run test:e2e -- apps/web/e2e/matching.spec.ts

2026-09-07：2/2 通过，40.5 秒。仅复测两条受影响匹配用例。

双页用例通过可控请求门保持真实匹配 GET 处于加载状态，在表单准备已开始后释放；页面完成自动展开后正常进入编辑，随后仍完成真实服务器版本冲突拒绝、草稿保留及暂停发布验证。未增加重试、任意等待时长或修改服务器响应。

失败时的证据位于 frontend-e2e-loading-race-before；此目录保留修复后报告与成功截图。
