前端自动化回归记录

日期：2026-09-07。全部运行均使用独立 PostgreSQL、API 与 Vite，端口 5179，解析器为 local。未触及 5181 人工浏览器栈或 5173 日常数据。

- 完整回归：24/24 通过，1.9 分钟，见 frontend-e2e-initial。
- 新增能力早成稿、可选问答：1/1 通过，14.4 秒，见 frontend-e2e-optional。
- 修正元素截图固定导航遮挡后复测双端匹配：1/1 通过，21.1 秒，见 frontend-e2e-matching-capture。
- 精简重复聊天草稿、显露折叠硬条件后复测：3/3 通过，39.5 秒，见 frontend-e2e-presentation。
- 单张匹配结果铺满桌面结果区后复测：1/1 通过，38.3 秒，见 frontend-e2e-final-matching；该目录保存最终匹配截图。
- 收尾复测发现测试在初始加载时误走旧入口，原始 1 过 1 失败证据见 frontend-e2e-loading-race-before。修复等待逻辑并加入挂起初次 GET 的覆盖后，两条匹配用例 2/2 通过，40.5 秒，见 frontend-e2e-loading-race-after。

共有 25 个不同用例通过，收尾发现的测试加载时序失败已修复并复测通过；保留前后 HTML 报告、失败截图和 trace、结果摘要与成功匹配的桌面及 390px 截图。最后一次前端单元测试 15/15、lint、build 全部通过，另覆盖不足 400 字的历史结构草稿折叠以及普通短回复保留。此结果不替代真实 Qwen 解析评估、示例数据库内容审查或生产验收。

新增用例检查：示例仅填入且可编辑、示例误发被阻止、未发送文字不被覆盖、单段需求直接提炼、快捷确认保护未发送修改、确认后自动预填匹配、私密示例不发布、核心能力事实齐全即成稿、主动可选问答可随时返回草稿且原事实不丢。

既有测试继续检查真实双端发布和撤回、版本冲突、丢响应恢复、身份固定、持久化、咨询、可访问性与附件。
