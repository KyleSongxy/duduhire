收尾复测发现的测试时序问题

需求方双页发布冲突用例在 `preparePublication` 内调用瞬时 `edit.isVisible()`，把初始加载中的编辑按钮误判为未展开状态，转而等待已经不存在的旧入口“查找合适的人才”。失败页面最终已经显示正常可用的“修改展示资料”，并非产品无法进入编辑。

原始结果：匹配用例 1 通过、1 失败。保留原始 run.log、HTML 报告、失败截图、error-context 和 trace。

修复：测试直接等待自动展开后的真实编辑按钮或预览表单，移除旧入口推断；双页用例主动挂起初次匹配 GET 来覆盖等待加载的场景。不修改产品代码处理这次时序问题。
