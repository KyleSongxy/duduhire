# 产品动效片文案核对 v1

2026-09-09。核对范围：当前 `assets/manifest.json`、`creative-brief.md`、PNG 联系表及输入、确认后重点图层。只读素材；未运行 Blender，未判断正在渲染的 1320 帧或最终视频是否通过。

本文件保留初次核对的问题历史。以下两项已按当前文件修复；现无待处理的文案一致性事项。本结论只覆盖文案和静态素材，不代替补渲染后的成片检查。

## 1. 已修复（原 P2）：标成原句的引文与输入尚非逐字一致

修复记录：经历输入已改为三行“做过内部知识库。” / “我负责文档整理、检索与评测；” / “同事负责权限和上线。可提供脱敏报告。”。后续依据卡及三张确认卡保持不动。已验证忽略换行后，依据引文是输入中的连续原文子串；新输入图层已交由主代理补渲染。`manifest.json` 已同步三行文字和新文本边界。

受影响的内容：`talent-input.png` / `talent-input-text.png` 与 `evidence-panel.png`、三张 `confirm-*.png`。

修复前输入为：

> 做过内部知识库，负责文档整理、检索与评测。
> 可提供脱敏报告；同事负责权限和上线。

依据面板标为“来源原句”，确认面板也使用引号，引用的却是：

> 我负责文档整理、检索与评测；同事负责权限和上线。

当时个人和团队的职责含义保持一致，绿色高亮也已正确对应个人职责；发现的问题是引用形式：增加“我”、改变断句，并拼接了输入中被“可提供脱敏报告”隔开的内容。影片强调原句与依据可追溯，因此不能把这个整理句当作逐字原句。

原修复建议（已执行为上述三行版本）：只更新输入片段，让现有依据成为前文的连续原文节选；或者保留输入，改为忠实的分段节选并明确省略。此次实际采用前一种方式，并将第一句单独排成一行以便阅读。

## 2. 已修复（原 P3）：提案正文尚未同步最终采用范围及制作阶段

修复记录：`creative-brief.md` 主表现已采用“看清工作与能力”的资料对照镜头，明确以能力档案演示三态修改及确认，需求方在末镜以确认后的示例摘要出现。第 7、9 节已移除等待批准后制作的表述，区分当前本地制作、既有静态检查与合成后仍需完成的成片检查。

文件：`creative-brief.md`。以下为修复前记录：

- 第 5 节主表 34–40 秒仍为“看懂为何相关”的私密匹配结果版本，包含“相关之处”“还需确认”和匹配结果声明。当前 `compare-demand.png` 与 `compare-capability.png` 明确为两个示例摘要的并列对照，manifest 均为 `is_matching_result: false`。第 94 行已允许采用“看清工作与能力”备选版；需要把已选择的版本写回主表，不能让交付文档继续暗示本片展示了真实匹配输出。
- 第 5 节 28–34 秒仍写“示例两端资料分别完成本人确认”；当前三态确认 PNG 都是能力档案，只能作为一次“修改前 → 已修改、待确认 → 用户已确认”的操作示范。需求方在末镜作为已确认的示例摘要出现，不应声称其单独确认操作也在本片中展示。可改写为“以能力档案演示修改与确认，末镜并置双方确认后的示例摘要”。
- 第 7 节和第 9 节末仍写“当前阶段只交付文稿与参考证据”“待当前文稿与方向确认后进入制作”，与文首的“用户已确认、正在制作独立预览”不一致。可保留提案与参考分析的历史事实，但交付前应把当前阶段改为实际已完成和仍未验证的事项。

以上文档修正不要求增加匹配素材、补造需求方确认记录或扩大片长。

## 本次写入边界

初次核对仅新增本文件，未修改渲染内容。后续三行输入和文稿修复在主代理明确授权下完成。本次状态收尾仅更新本报告与 `assets/contact-sheet.jpg` 文案联系表，未改任何渲染 PNG、生成脚本、场景或时间轴。
